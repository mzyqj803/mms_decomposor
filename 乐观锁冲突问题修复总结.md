# 乐观锁冲突问题修复总结

## 问题现象

用户点击"执行工艺分解"按钮时，前端显示错误：
```
工艺分解失败: could not execute statement [(conn=5) Record has changed since last read in table 'containers']
```

## 问题分析

通过查看后端日志，发现完整的错误堆栈：

1. **工艺分解正常完成**：所有31个容器都成功分解完成
2. **批量更新状态成功**：所有容器的status都成功更新为1（已分解）
3. **错误发生位置**：在生成汇总表（STEP 3: Generate breakdown summary table）时
4. **具体错误点**：调用 `containersRepository.findByContractId(contractId)` 时

### 根本原因

这是一个**Hibernate自动flush导致的乐观锁冲突**：

1. 在STEP 2中，批量更新了所有containers的status
2. 这些container对象被修改后留在了Hibernate的持久化上下文（EntityManager）中
3. 在STEP 3执行查询时，Hibernate会尝试自动flush这些已修改的对象
4. 但是这些对象的 `last_update_ts` 字段已经过时（因为批量保存时数据库已经更新了时间戳）
5. 当Hibernate尝试flush这些对象时，乐观锁检查失败，抛出异常

## 修复方案

在批量更新containers的status之后，调用 `entityManager.clear()` 清除持久化上下文，避免后续查询时触发auto-flush。

### 代码修改

**文件**：`src/main/java/com/mms/service/impl/BreakdownServiceImpl.java`

#### 1. 添加EntityManager注入

```java
import jakarta.persistence.EntityManager;
import jakarta.persistence.PersistenceContext;

@Service
@RequiredArgsConstructor
@Slf4j
public class BreakdownServiceImpl implements BreakdownService {
    
    // ... 其他字段 ...
    
    @PersistenceContext
    private EntityManager entityManager;
    
    // ...
}
```

#### 2. 在批量更新后清除EntityManager

```java
// 批量更新容器状态
BreakdownServiceImpl selfProxy = applicationContext.getBean(BreakdownServiceImpl.class);
selfProxy.batchUpdateContainersStatus(containerIdList, 1);

log.info("Batch update container status completed, took: {}ms", 
    System.currentTimeMillis() - updateStatusStartTime);

// ✅ 清除EntityManager避免乐观锁冲突
// 批量更新后，内存中的container对象的last_update_ts已经过时
// 当再次查询时，Hibernate会尝试auto-flush并导致乐观锁错误
entityManager.clear();
log.debug("EntityManager cleared after batch update to avoid optimistic lock conflicts");
```

#### 3. 同时修复的另一个问题

在保存问题件时，也可能遇到类似问题，因此重新获取freshContainer：

```java
// 保存问题部件到问题件表
// 重新获取container以避免乐观锁冲突
Containers freshContainer = containersRepository.findById(containerId)
    .orElseThrow(() -> new RuntimeException("箱包不存在"));

ContainerComponentsBreakdownProblems problem = new ContainerComponentsBreakdownProblems();
problem.setContainer(freshContainer);  // 使用fresh的container对象
// ... 其他字段设置 ...
problemsRepository.save(problem);
```

## 测试验证

### 编译和部署

1. **停止后端服务**
   ```bash
   docker-compose stop backend
   ```

2. **重新编译**
   ```bash
   mvn clean package -DskipTests
   ```
   - ✅ 编译成功，耗时 10.574秒

3. **重新构建镜像**
   ```bash
   docker-compose build --no-cache backend
   ```
   - ✅ 镜像构建成功

4. **启动服务**
   ```bash
   docker-compose up -d backend
   ```
   - ✅ 服务启动成功并健康运行

### 验证状态

```bash
docker-compose ps
```

结果：
```
NAME           STATUS                    PORTS
mms-backend    Up 28 seconds (healthy)   0.0.0.0:8080->8080/tcp
mms-frontend   Up 9 minutes              0.0.0.0:9000->80/tcp
mms-mariadb    Up 9 minutes (healthy)    0.0.0.0:3307->3306/tcp
mms-redis      Up 9 minutes (healthy)    0.0.0.0:6379->6379/tcp
```

所有服务正常运行！

## 技术要点

### 乐观锁机制

JPA/Hibernate使用 `@LastModifiedDate` 或 `@Version` 字段实现乐观锁：
- 更新记录时，检查 `last_update_ts` 是否与查询时相同
- 如果不同，说明记录被其他事务修改过，抛出异常
- 这是为了防止丢失更新（Lost Update）问题

### Hibernate自动flush

Hibernate在执行查询前会自动flush持久化上下文中的修改：
- 这是为了确保查询结果与当前内存状态一致
- 但在我们的场景中，这导致了乐观锁冲突

### EntityManager.clear()

`clear()` 方法的作用：
- 清除持久化上下文中的所有实体
- 后续查询会从数据库重新加载
- 避免了过时对象导致的乐观锁冲突

## 类似问题的通用解决方案

如果遇到类似的乐观锁冲突，可以考虑：

1. **方案1：清除EntityManager** (推荐)
   ```java
   entityManager.clear();
   ```

2. **方案2：重新查询对象**
   ```java
   Containers fresh = repository.findById(id).get();
   ```

3. **方案3：分离实体**
   ```java
   entityManager.detach(entity);
   ```

4. **方案4：使用@Modifying注解**
   ```java
   @Modifying(clearAutomatically = true)
   @Query("UPDATE ...")
   ```

5. **方案5：使用独立事务**
   ```java
   @Transactional(propagation = Propagation.REQUIRES_NEW)
   ```

## 总结

- ✅ 问题根本原因：Hibernate自动flush + 乐观锁冲突
- ✅ 修复方案：在批量更新后清除EntityManager
- ✅ 编译部署：成功，无错误
- ✅ 服务状态：健康运行

现在可以正常执行工艺分解功能了！建议用户再次测试以确认问题已解决。

