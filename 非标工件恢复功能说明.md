# 非标工件自动恢复功能说明

## 问题背景

在工艺分解过程中，系统会自动创建非标工件（如 `TTA0E104002~AA79375`）。但存在以下问题：

**原有逻辑**：
- 如果非标工件已存在且status=1（active），直接使用
- 如果非标工件不存在，基于基础组件创建
- **问题**：如果非标工件被删除过（status=0），系统会认为它已存在而不再创建，导致工艺分解失败

## 需求

当工艺分解时遇到已删除的非标工件（status=0），应该：
1. 自动恢复该非标工件（status改为1）
2. 更新其name字段（基于当前基础组件的name）
3. 更新缓存

## 实现方案

### 修改位置

文件：`src/main/java/com/mms/service/impl/BreakdownServiceImpl.java`  
方法：`getOrCreateNonStandardComponent(String nonStandardCode)`

### 实现细节

#### 1. 第一次快速检查（无锁）

```java
// 先查找active的组件
Optional<Components> existingComponent = componentsRepository.findActiveByComponentCode(nonStandardCode);
if (existingComponent.isPresent()) {
    log.info("Non-standard component already exists and is active (first check): {}", nonStandardCode);
    return existingComponent;
}

// 检查是否存在已删除的非标组件（status=0）
Optional<Components> deletedComponent = componentsRepository.findByComponentCode(nonStandardCode);
if (deletedComponent.isPresent() && deletedComponent.get().getStatus() == 0) {
    log.info("Found deleted non-standard component, will restore it: {}", nonStandardCode);
    // 继续执行加锁逻辑
}
```

**说明**：
- 第一次检查只查找active的组件，如果存在直接返回
- 如果没有active的，检查是否有deleted的
- 如果发现deleted的组件，继续进入加锁逻辑进行恢复

#### 2. 第二次检查（在事务中，有锁）

```java
// 在新事务中再次检查active的组件
existingComponent = componentsRepository.findActiveByComponentCode(nonStandardCode);
if (existingComponent.isPresent()) {
    transactionManager.commit(status);
    return existingComponent;
}

// 检查是否存在已删除的非标组件，如果存在则恢复它
Optional<Components> checkDeletedOpt = componentsRepository.findByComponentCode(nonStandardCode);
if (checkDeletedOpt.isPresent()) {
    Components checkDeleted = checkDeletedOpt.get();
    if (checkDeleted.getStatus() == 0) {
        log.info("Found deleted non-standard component, restoring it: {}", nonStandardCode);
        
        // 提取基础组件代码以获取最新的name
        String baseComponentCode = nonStandardCode.substring(0, nonStandardCode.indexOf("~"));
        Optional<Components> baseComponentOpt = componentsRepository.findActiveByComponentCode(baseComponentCode);
        
        if (baseComponentOpt.isPresent()) {
            Components baseComponent = baseComponentOpt.get();
            
            // ✅ 恢复组件：设置status=1，更新name
            checkDeleted.setStatus(1);
            checkDeleted.setName(baseComponent.getName());
            Components restoredComponent = componentsRepository.save(checkDeleted);
            
            // 更新缓存
            try {
                String componentJson = objectMapper.writeValueAsString(restoredComponent);
                componentCacheService.putComponentToCache(nonStandardCode, componentJson);
            } catch (Exception cacheEx) {
                log.warn("Failed to cache restored component: {}, error: {}", 
                    nonStandardCode, cacheEx.getMessage());
            }
            
            transactionManager.commit(status);
            log.info("Non-standard component restored successfully: code={}, new name={}", 
                nonStandardCode, baseComponent.getName());
            return Optional.of(restoredComponent);
        } else {
            log.error("Base component not found or deleted, cannot restore: baseComponentCode={}", baseComponentCode);
            transactionManager.rollback(status);
            return Optional.empty();
        }
    }
}
```

**关键操作**：
1. 查找基础组件获取最新的name
2. 将deleted组件的status设置为1
3. 更新name为基础组件的当前name
4. 保存并提交事务
5. 更新Redis缓存

#### 3. 并发冲突重试逻辑

在唯一约束冲突后的重试中，也添加了恢复deleted组件的逻辑：

```java
// 先查找active的组件
Optional<Components> existing = componentsRepository.findActiveByComponentCode(nonStandardCode);
if (existing.isPresent()) {
    transactionManager.commit(queryStatus);
    return existing;
}

// 查找所有状态的组件，可能是已删除的
Optional<Components> anyStatus = componentsRepository.findByComponentCode(nonStandardCode);
if (anyStatus.isPresent() && anyStatus.get().getStatus() == 0) {
    // 恢复已删除的组件
    Components toRestore = anyStatus.get();
    String baseComponentCode = nonStandardCode.substring(0, nonStandardCode.indexOf("~"));
    Optional<Components> baseComponentOpt = componentsRepository.findActiveByComponentCode(baseComponentCode);
    
    if (baseComponentOpt.isPresent()) {
        toRestore.setStatus(1);
        toRestore.setName(baseComponentOpt.get().getName());
        Components restoredComponent = componentsRepository.save(toRestore);
        transactionManager.commit(queryStatus);
        log.info("Restored deleted component after conflict: {}", nonStandardCode);
        return Optional.of(restoredComponent);
    }
}
```

## 使用场景示例

### 场景1：首次创建非标工件

1. 工艺分解遇到 `TTA0E104002~AA79375`
2. 查询发现不存在
3. 基于 `TTA0E104002` 创建新的非标工件
4. status=1, name从基础组件复制

### 场景2：非标工件已存在且active

1. 工艺分解遇到 `TTA0E104002~AA79375`
2. 查询发现已存在且status=1
3. 直接使用现有组件

### 场景3：非标工件已被删除（本次新增）

1. 工艺分解遇到 `TTA0E104002~AA79375`
2. 查询发现已存在但status=0
3. ✅ **自动恢复**：
   - 将status改为1
   - 从基础组件`TTA0E104002`获取最新name并更新
   - 更新缓存
4. 返回恢复后的组件

### 场景4：基础组件name已更新（本次新增）

1. 某个非标工件 `TTA0E104002~AA79375` 之前被删除
2. 同时基础组件 `TTA0E104002` 的name已经更新
3. 工艺分解时检测到deleted的非标工件
4. ✅ **恢复时更新name**：
   - status: 0 → 1
   - name: 旧名称 → 基础组件的新名称

## 技术要点

### 1. 线程安全

- 使用 `synchronized(lock)` 确保同一个非标工件代码只有一个线程在处理
- 使用编程式事务（PROPAGATION_REQUIRES_NEW）确保能看到最新提交的数据

### 2. 双重检查机制

- 第一次检查（快速路径，无锁）
- 第二次检查（在锁内、事务中）
- 确保并发场景下的正确性

### 3. 异常处理

- 唯一约束冲突后的重试机制
- 重试时也会检查并恢复deleted的组件

### 4. 缓存同步

- 恢复组件后立即更新Redis缓存
- 缓存失败不影响主流程

## 部署信息

### 编译状态
- ✅ 编译成功（耗时：10.189秒）
- ✅ 无linter错误

### 镜像构建
- ✅ Docker镜像重新构建成功

### 服务状态
- ✅ mms-backend: Healthy
- ✅ mms-frontend: Running
- ✅ mms-mariadb: Healthy
- ✅ mms-redis: Healthy

## 测试建议

### 测试步骤

1. **准备测试数据**
   ```sql
   -- 创建一个非标工件并将其删除
   UPDATE components 
   SET status = 0 
   WHERE component_code = 'TTA0E104002~AA79375';
   
   -- 可选：更新基础组件的name
   UPDATE components 
   SET name = '新的测试名称' 
   WHERE component_code = 'TTA0E104002';
   ```

2. **执行工艺分解**
   - 选择包含该非标工件的合同
   - 点击"执行工艺分解"按钮

3. **验证结果**
   ```sql
   -- 检查非标工件是否被恢复
   SELECT id, component_code, name, status 
   FROM components 
   WHERE component_code = 'TTA0E104002~AA79375';
   
   -- 期望结果：
   -- status = 1
   -- name = 新的测试名称（如果基础组件name已更新）
   ```

4. **查看日志**
   ```bash
   docker-compose logs backend | grep "restored"
   ```
   
   应该看到类似日志：
   ```
   Non-standard component restored successfully: code=TTA0E104002~AA79375, new name=新的测试名称
   ```

## 总结

✅ **功能增强**：非标工件自动恢复功能  
✅ **解决问题**：已删除的非标工件不会阻止工艺分解  
✅ **额外优化**：恢复时同步更新name字段  
✅ **部署完成**：服务正常运行

用户现在可以放心删除非标工件，工艺分解时会自动恢复它们！

