# 零部件删除逻辑对比说明

## 修改前（物理删除，有检查）

```java
@Override
@Transactional
public void deleteComponent(Long id) {
    Components component = getComponentById(id);
    
    // ❌ 检查是否有关联的工艺或关系数据
    if (component.getProcesses() != null && !component.getProcesses().isEmpty()) {
        throw new RuntimeException("该零部件存在工艺数据，不能删除");
    }
    if (component.getChildren() != null && !component.getChildren().isEmpty()) {
        throw new RuntimeException("该零部件存在子组件关系，不能删除");
    }
    
    // ❌ 物理删除规格数据
    componentsSpecRepository.deleteByComponentId(id);
    
    // ❌ 物理删除父子关系（作为子组件的记录）
    componentsRelationshipRepository.deleteByChildId(id);
    
    if (component.getParents() != null && !component.getParents().isEmpty()) {
        throw new RuntimeException("该零部件存在父组件关系，不能删除");
    }
    
    // ❌ 物理删除组件
    componentsRepository.deleteById(id);
    
    // 清除相关缓存
    clearComponentCache(id);
    clearComponentsCache();
    
    log.info("删除零部件成功: {}", component.getComponentCode());
}
```

**问题**：
- ❌ 有子组件不能删除
- ❌ 被其他组件引用不能删除
- ❌ 有工艺数据不能删除
- ❌ 删除后数据完全丢失，无法恢复

---

## 修改后（软删除，无检查）

```java
@Override
@Transactional
public void deleteComponent(Long id) {
    Components component = componentsRepository.findById(id)
        .orElseThrow(() -> new RuntimeException("零部件不存在"));
    
    // ✅ 软删除：将 status 设置为 0 (deleted)
    component.setStatus(0);
    componentsRepository.save(component);
    
    // 清除相关缓存
    clearComponentCache(id);
    clearComponentsCache();
    
    log.info("软删除零部件成功: {}, status设置为0", component.getComponentCode());
}
```

**优点**：
- ✅ **无任何限制**：不检查工艺、子组件、父组件等任何引用关系
- ✅ **数据保留**：组件及其所有关联数据（规格、工艺、父子关系）都保留在数据库
- ✅ **可恢复**：需要时可以将 status 改回 1 来恢复组件
- ✅ **自动隐藏**：由于查询时过滤 status=0，删除的组件不会在任何列表中显示
- ✅ **安全便捷**：用户可以随时删除任何组件，不会遇到"不能删除"的错误

---

## 删除后的影响

### 1. 列表查询
```sql
-- 自动过滤，不显示已删除的组件
SELECT * FROM components WHERE status = 1;
```

### 2. 工艺分解
- 不会匹配到已删除的组件（status=0）
- 如果容器组件引用了已删除的组件，会记录为"问题部件"

### 3. 父子关系显示
- 查询父子关系时自动过滤已删除的组件
- 即使关系数据存在，前端也不会显示已删除的组件

### 4. 数据恢复
如需恢复某个组件：
```sql
-- 简单地将 status 改回 1
UPDATE components SET status = 1 WHERE id = ?;
```
恢复后，该组件及其所有关联数据（规格、工艺、父子关系）立即可用。

---

## 使用示例

### 场景1：删除有子组件的父组件
**修改前**：❌ 报错"该零部件存在子组件关系，不能删除"  
**修改后**：✅ 直接删除，父组件 status=0，子组件关系保留但隐藏

### 场景2：删除被其他组件引用的组件
**修改前**：❌ 报错"该零部件存在父组件关系，不能删除"  
**修改后**：✅ 直接删除，组件 status=0，引用关系保留但隐藏

### 场景3：删除有工艺数据的组件
**修改前**：❌ 报错"该零部件存在工艺数据，不能删除"  
**修改后**：✅ 直接删除，组件和工艺数据都保留但隐藏

### 场景4：误删除后需要恢复
**修改前**：❌ 数据已物理删除，无法恢复  
**修改后**：✅ 执行 SQL 将 status 改回 1，所有数据立即恢复

---

## 总结

软删除方案的核心理念：
1. **删除 = 隐藏**，而不是真正删除数据
2. **不设限制**，用户可以删除任何组件
3. **可恢复**，误删除可以轻松恢复
4. **保留历史**，所有关联关系都完整保留

这种方式更加安全、灵活，适合生产环境使用。

